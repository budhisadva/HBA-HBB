Necesario crear maquina virtual:
$python3 -m venv <nombre de la maquina>

Activar la maquina:
$. <nombre de la maquina>/bin/activate

Instalar dependencias:
$pip install -r requirements.txt


(la terminal necesita estar ubicada en /HBA-HBB)
